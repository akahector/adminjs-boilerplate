import { ThemeOverride } from '@adminjs/design-system';

const primaryColor = '#7B0C33'; // Your desired primary color (e.g., purple)

const CustomTheme: ThemeOverride = {
  colors: {
    primary100:primaryColor,
    primary20:primaryColor,
    primary40:primaryColor,
    primary60:primaryColor,
    primary80:primaryColor,
  },
};

export default CustomTheme;